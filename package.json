{
  "name": "translate-app",
  "version": "1.0.0",
  "main": "server/index.js",
  "license": "UNLICENSED",
  "scripts": {
    "shopify": "shopify",
    "build": "shopify app build",
    "dev": "shopify app dev",
    "info": "shopify app info",
    "generate": "shopify app generate",
    "deploy": "shopify app deploy",
    "config-push": "npm run shopify app config push"
  },
  "dependencies": {
    "@shopify/app": "3.51.2",
    "@shopify/cli": "3.51.2"
  },
  "author": "zak",
  "private": true,
  "workspaces": [
    "extensions/*",
    "server",
    "web"
  ]
}